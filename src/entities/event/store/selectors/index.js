import {
    getCurrentEventId,
    getCurrentEventMeta,
    getCurrentEventNavData,
    getCurrentEventRaces,
    getCurrentEventRating,
} from './selectors';

export const selectors = {
    getCurrentEventId,
    getCurrentEventMeta,
    getCurrentEventNavData,
    getCurrentEventRaces,
    getCurrentEventRating,
};
